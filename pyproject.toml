[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "langchain-stackoverflow"
version = "0.1.0"
description = "LangChain retriever integration for Stack Overflow — search questions and retrieve full answers from the Stack Exchange network."
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "Farhan" },
]
keywords = ["langchain", "stackoverflow", "retriever", "rag", "ai", "llm"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "langchain-core>=0.2.0",
    "requests>=2.28.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
    "ruff>=0.4.0",
    "mypy>=1.0",
]

[project.urls]
Homepage = "https://github.com/urraf/langchain-stackoverflow"
Repository = "https://github.com/urraf/langchain-stackoverflow"
Documentation = "https://github.com/urraf/langchain-stackoverflow#readme"
"Bug Tracker" = "https://github.com/urraf/langchain-stackoverflow/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/langchain_stackoverflow"]

[tool.ruff]
target-version = "py39"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B", "SIM"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.mypy]
python_version = "3.9"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
